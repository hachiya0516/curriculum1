問1
スイッチ番号→Switch3


問2
ポートの役割
→switch1(F0/1)指定ポート
理由：ルートポートの向かいのポートのため。
(F0/2)ルートポート
理由：ルートブリッジに隣接しているため。

switch2(F0/1）→ルートポート
理由：ルートパスコストの方が値が小さいため。（19＋100＝119）
(F0/2)→ブロッキングポート
理由：ルートポート、指定ポートのどちらにも選出されなかったため

switch3（E0/1)→指定ポート
理由：ルートブリッジの全てのポートは必ず指定ポートになるため
(E0/2)→指定ポート
理由：ルートブリッジの全てのポートは必ず指定ポートになるため

switch4(E0/1)→指定ポート
理由：各コリジョンドメインごとに1つ指定ポートが存在するため
(E0/2)→ルートポート
理由：ルートパスコストの方が値が小さいため。（100）
